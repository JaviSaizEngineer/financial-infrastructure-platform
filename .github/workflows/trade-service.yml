name: Trade Service CI

on:
  push:
    paths:
      - "trade-service/**"
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - run: |
          cd trade-service
          chmod +x mvnw
          ./mvnw clean package -DskipTests

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - run: |
          docker build -t javisaizengineer/trade-service:${{ github.run_number }} ./trade-service
          docker push javisaizengineer/trade-service:${{ github.run_number }}