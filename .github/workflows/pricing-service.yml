name: Pricing Service CI

on:
  push:
    paths:
      - "pricing-service/**"
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - run: |
          cd pricing-service
          chmod +x mvnw
          ./mvnw clean package -DskipTests

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - run: |
          docker build -t javisaizengineer/pricing-service:${{ github.run_number }} ./pricing-service
          docker push javisaizengineer/pricing-service:${{ github.run_number }}