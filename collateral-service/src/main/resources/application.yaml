spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/collateral
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics