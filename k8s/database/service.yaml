apiVersion: apps/v1
kind: Deployment
metadata:
  name: collateral-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: collateral-db
  template:
    metadata:
      labels:
        app: collateral-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: collateral
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres