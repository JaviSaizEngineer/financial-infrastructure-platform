apiVersion: apps/v1
kind: Deployment
metadata:
  name: trade-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trade-service
  template:
    metadata:
      labels:
        app: trade-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: trade-service
          image: javisaizengineer/trade-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080