server:
  port: 8080

spring:
  application:
    name: trade-service

  datasource:
    url: jdbc:postgresql://postgres:5432/financial
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true